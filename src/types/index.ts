export type Attributes =  {
  bg?: string;
  value?: string;
  bold?: boolean;
  dim?: boolean;
  italic?: boolean;
  underline?: boolean;
  inverse?: boolean;
  hidden?: boolean;
  strikethrough?: boolean;
  visible?: boolean;
};

export type Rgb = [number, number, number];
